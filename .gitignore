# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Capacitor — the native projects are generated by `npx cap add`, and the build
# outputs inside them are pure derivatives of `dist/`.
android/app/src/main/assets/public
android/app/build
android/build
android/.gradle
android/local.properties
android/app/release
ios/App/App/public
ios/App/Pods
ios/App/build
ios/DerivedData
ios/App/App.xcworkspace/xcuserdata
ios/App/App.xcodeproj/xcuserdata
*.keystore
!debug.keystore

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Environment variables
.env
.env.local
.env.*.local
skills/
.claude/
.cursor/
.codex/
.trae/
.void/
.windsurf/
.zed/
.zencoder/
.opencode/
# capacitor-assets regenerates these on every run; the committed
# public/manifest.webmanifest is hand-corrected (see CLAUDE.md).
/icons/

# Locally generated dev TLS certificate (npm run cert:lan) — machine-specific.
certs/

# Superseded planning/scratch prompts — kept on disk, out of the repo.
# Finalplan.md replaces them; CLAUDE.md and Design.md are the other keepers.
/task.md
/task2.md
/Admin task.md
/Audit.md
/REBUILD_PROMPT.md
